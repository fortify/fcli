---
name: Change-Me                 # Required: Report template name
description: Change-Me          # Optional: Report template description
type: ISSUE                     # Required: ISSUE, PROJECT or PORTFOLIO
parameters:
- name: Change-Me0              # Required: Report parameter name
  description: Change-Me0       # Optional: Report parameter description
  identifier: Change-Me0        # Required: ID/name of the report parameter in the BIRT template.
  type: SINGLE_SELECT_DEFAULT   # Required: BOOLEAN, MULTI_PROJECT, PROJECT_ATTRIBUTE, SINGLE_PROJECT, SINGLE_SELECT_DEFAULT, STRING, or DATE
  paramOrder: 0                 # Optional: Report parameter ordering value
  reportParameterOptions:       # Required for parameter type "SINGLE_SELECT_DEFAULT", not allowed for other parameter types
  - defaultValue: true          # Required: Boolean indicating whether this is the default parameter option
    description: Change-Me1     # Optional: Report parameter option description
    displayValue: Change-Me1    # Required: Report parameter display value as shown in SSC UI
    reportValue: Change-Me1     # Required: The value to be passed to the BIRT template
  - defaultValue: false         # See above
    description: Change-Me2     # See above
    displayValue: Change-Me2    # See above
    reportValue: Change-Me2     # See above
- name: Change-Me3              # Repeated section above for additional report parameter but different type 
  paramOrder: 1
  description: Change-Me3
  identifier: Change-Me3
  type: STRING
