/**
 * This package defines top-level commands for creating, retrieving (getting), updating, deleting entities, as well as for uploading and
 * downloading files.
 * 
 * TODO Reorganize into two separate top-level packages 'crud' (for CRUD operations) and 'transfer' (for up/download), as that better
 *      describes the actual commands provided by the classes in these packages.
 */
package com.fortify.cli.common.command.entity;

