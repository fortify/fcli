name: Fortify on Demand Scan
run-name: "Fortify on Demand Scan (${{ github.event_name }})"

on:
  workflow_dispatch:
  push:
  pull_request:
  schedule:
    - cron: '16 0 * * 5'

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  check-duplicate-run:
    uses: fortify/shared-github/.github/workflows/reusable-check-duplicate-run.yml@dca231d1f376d592b730a4ede5c8da82dba9c0a7

  FoD-Scan:
    needs: check-duplicate-run
    if: needs.check-duplicate-run.outputs.should_skip != 'true'
    uses: fortify/shared-github/.github/workflows/reusable-fortify-analysis.yml@dca231d1f376d592b730a4ede5c8da82dba9c0a7
    with:
      java-version: '17'
    secrets: inherit
