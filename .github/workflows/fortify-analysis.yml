name: Fortify on Demand Scan
run-name: "Fortify on Demand Scan (${{ github.event_name }})"

on:
  workflow_dispatch:
  push:
  pull_request:
  schedule:
    - cron: '16 0 * * 5'

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  check-duplicate-run:
    uses: fortify/shared-github/.github/workflows/reusable-check-duplicate-run.yml@d9552c48d8270d8e320ff2ab4d356c8a69356686

  FoD-Scan:
    needs: check-duplicate-run
    if: needs.check-duplicate-run.outputs.should_skip != 'true'
    uses: fortify/shared-github/.github/workflows/reusable-fortify-analysis.yml@d9552c48d8270d8e320ff2ab4d356c8a69356686
    with:
      java-version: '17'
    secrets: inherit
