name: update-repo-docs

on:
  workflow_dispatch:
  schedule:
    - cron: '5 4 * * *'
  push:
    branches:
      - dev/v3.x
      
jobs:
  update-repo-docs:
    permissions:
      contents: write
      pull-requests: write
    uses: fortify/shared-github/.github/workflows/reusable-update-repo-docs.yml@dca231d1f376d592b730a4ede5c8da82dba9c0a7
